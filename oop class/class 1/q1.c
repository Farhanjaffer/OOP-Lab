//wriet a program to perform additiomn multiplication and subraction using differenet functions

#include <stdio.h>
int sum;
    int sub;
    int mult;
int main()
{
    
    int a,b;
    sum=a+b;
    sub=a-b;
    mult=a*b;

    printf("enter the value of a & b:\n");
    scanf("%d" "%d",&a,&b);

    printf("the sum is: %d\n",a+b);
 printf("the diff is: %d\n",a-b);
  printf("the product is: %d\n",a*b);

    
    return 0;
}